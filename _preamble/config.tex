\usepackage{etoolbox} % Permite lógica booleana e condicionais

% --- DEFINIÇÃO DAS VARIÁVEIS (GETTERS/SETTERS) ---
%> Variável booleana (true/false) para formatação
\newtoggle{ufvnorms}
\newcommand{\UseUFVNorms}[1]{%
    \ifstrequal{#1}{True}{\toggletrue{ufvnorms}}{%
    \ifstrequal{#1}{true}{\toggletrue{ufvnorms}}{\togglefalse{ufvnorms}}}%
}
%> Variável boolena (true/false) para o cabeçalho
\newtoggle{useheaders}
\newcommand{\UseHeaders}[1]{%
    \ifstrequal{#1}{True}{\toggletrue{useheaders}}{%
    \ifstrequal{#1}{true}{\toggletrue{useheaders}}{\togglefalse{useheaders}}}%
}
\newtoggle{numericformat}
\toggletrue{numericformat}
\newcommand{\NumericFormat}[1]{%
    \ifstrequal{#1}{True}{\toggletrue{numericformat}}{%
    \ifstrequal{#1}{true}{\toggletrue{numericformat}}{\togglefalse{numericformat}}}%
}
\newtoggle{pretextual}
\toggletrue{pretextual}
\newcommand{\PreTextual}[1]{%
    \ifstrequal{#1}{True}{\toggletrue{pretextual}}{%
    \ifstrequal{#1}{true}{\toggletrue{pretextual}}{\togglefalse{pretextual}}}%
}
%> Comandos para nomear o documento
\newcommand{\DocTitle}[1]{\def\doctitle{#1}}
\newcommand{\DocAuthor}[1]{\def\docauthor{#1}}
\newcommand{\DocAdvisor}[1]{\def\docadvisor{#1}}
\newcommand{\DocCoAdvisor}[1]{\def\doccoadvisor{#1}}
\newcommand{\DocModality}[1]{\def\docmodality{#1}}
\newcommand{\DocDegree}[1]{\def\docdegree{#1}}
\newcommand{\DocDate}[1]{\def\docdate{#1}}
\newcommand{\DocProgram}[1]{\def\docprogram{#1}}
\newcommand{\DocCenter}[1]{\def\doccenter{#1}}
\newcommand{\ReportCourse}[1]{\def\reportcourse{#1}}
\newcommand{\DocAbstract}[1]{\def\docabstract{#1}}
\newcommand{\DocResumo}[1]{\def\docresumo{#1}}
\newcommand{\DocKeywords}[1]{\def\dockeywords{#1}}
\newcommand{\DocPalavrasChave}[1]{\def\docpalavraschave{#1}}
\newcommand{\DocAcknowledgments}[1]{\def\docacknowledgments{#1}}
\newcommand{\DocEpigraph}[2]{\def\docepigraph{#1}\def\epigraphauthor{#2}}

%> Valores padrão (fallback) caso o usuário esqueça de preencher algo
\def\doctitle{Título não definido}
\def\docauthor{Autor não definido}
\def\doccoadvisor{} % Inicia vazio para o ifdefempty funcionar
\def\docmodality{Dissertação}
\def\docdegree{Mestre}
\def\docdate{\today}
\def\docprogram{Física}
\def\doccenter{Centro de Ciências Exatas}
\def\reportcourse{}
\def\docabstract{}
\def\docresumo{}
\def\dockeywords{}
\def\docpalavraschave{}
\def\docacknowledgments{}
\def\docepigraph{}
\def\epigraphauthor{}

%> Macro para construção do template
\newcommand{\BuildTemplateSetup}{%
    
    %> 1. Carrega os pacotes e ferramentas do usuário
    \input{_preamble/packages}
    \input{_preamble/standard_notation}

    %> 2. Carrega as opções de formatação: UFV / Publish
    \iftoggle{ufvnorms}{%
        \input{_preamble/format2ufv}     % Carrega o driver da UFV
    }{%
        \input{_preamble/format2publish} % Carrega o driver fancy
    }

    %> 3. Carrega o hyperref e bibtex
    \usepackage{hyperref}
    \iftoggle{ufvnorms}{%
        \hypersetup{
            colorlinks=true, 
            allcolors=black
        }
    }{%
        \hypersetup{
            colorlinks=true,
        	linkcolor=NavyBlue,
        	urlcolor=MediumBlue,
        	citecolor=ForestGreen,
        	pdfhighlight=/N
        }

    }
    \addbibresource{_bibliography/references.bib}

    %> 4. Metadados no PDF e no Título do LaTeX
    \hypersetup{
        pdftitle={\doctitle},
        pdfauthor={\docauthor}
    }
    \title{\doctitle}
    \author{\docauthor}

    %> 5. Carregar os macros pré-textuais
    \input{_title/generalmacros}
}